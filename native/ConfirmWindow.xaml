<Window x:Class="NexusModsDownloader.ConfirmWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="确认"
        Icon="ico.png"
        Width="500"
        MinHeight="240"
        MaxHeight="560"
        SizeToContent="Height"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="Transparent"
        FontFamily="Microsoft YaHei">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0" ResizeBorderThickness="0" GlassFrameThickness="0" />
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <materialDesign:BundledTheme BaseTheme="Light" PrimaryColor="Blue" SecondaryColor="Lime" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Light.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Defaults.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignColors;component/Themes/Recommended/Primary/MaterialDesignColor.Blue.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignColors;component/Themes/Recommended/Accent/MaterialDesignColor.Blue.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Border CornerRadius="8" Background="{DynamicResource MaterialDesignPaper}" BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="42" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Background="{DynamicResource PrimaryHueMidBrush}" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <TextBlock x:Name="TitleTextBlock" Foreground="White" FontWeight="SemiBold" VerticalAlignment="Center" Margin="18,0,0,0" />
                <Button Style="{StaticResource MaterialDesignIconButton}" Width="36" Height="36" Padding="6" HorizontalAlignment="Right" Margin="0,3,8,3" Click="Cancel_Click">
                    <materialDesign:PackIcon Kind="Close" Width="20" Height="20" Foreground="White" />
                </Button>
            </Grid>

            <Grid Grid.Row="1" Margin="24,22,24,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <ScrollViewer MaxHeight="390" VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="MessageTextBlock" TextWrapping="Wrap" FontSize="14" />
                </ScrollViewer>
                <CheckBox x:Name="OptionCheckBox" Grid.Row="1" Margin="0,8,0,14" Visibility="Collapsed" />
                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="ConfirmButton" Click="Confirm_Click" />
                    <Button Margin="8,0,0,0" Click="Cancel_Click" Content="取消" />
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
