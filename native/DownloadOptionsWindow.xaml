<Window x:Class="NexusModsDownloader.DownloadOptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="下载选项"
        Icon="ico.png"
        Height="300"
        Width="620"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="Transparent"
        FontFamily="Microsoft YaHei">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0"
                            ResizeBorderThickness="0"
                            GlassFrameThickness="0" />
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <materialDesign:BundledTheme BaseTheme="Light" PrimaryColor="Blue" SecondaryColor="Lime" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Light.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Defaults.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignColors;component/Themes/Recommended/Primary/MaterialDesignColor.Blue.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignColors;component/Themes/Recommended/Accent/MaterialDesignColor.Blue.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource MaterialDesignTextBox}">
                <Setter Property="FontFamily" Value="Microsoft YaHei" />
            </Style>

            <Style TargetType="{x:Type CheckBox}" BasedOn="{StaticResource MaterialDesignCheckBox}">
                <Setter Property="FontFamily" Value="Microsoft YaHei" />
            </Style>

            <Style TargetType="{x:Type Button}" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                <Setter Property="FontFamily" Value="Microsoft YaHei" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
            </Style>

            <Style x:Key="IconButtonPanelStyle" TargetType="{x:Type StackPanel}">
                <Setter Property="Orientation" Value="Horizontal" />
                <Setter Property="VerticalAlignment" Value="Center" />
                <Style.Resources>
                    <Style TargetType="{x:Type materialDesign:PackIcon}">
                        <Setter Property="VerticalAlignment" Value="Center" />
                    </Style>
                    <Style TargetType="{x:Type TextBlock}" BasedOn="{StaticResource MaterialDesignTextBlock}">
                        <Setter Property="VerticalAlignment" Value="Center" />
                        <Setter Property="Margin" Value="0,-1,0,0" />
                    </Style>
                </Style.Resources>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Border CornerRadius="8"
            Background="{DynamicResource MaterialDesignPaper}"
            BorderBrush="{DynamicResource MaterialDesignDivider}"
            BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="42" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Background="{DynamicResource PrimaryHueMidBrush}"
                  MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <TextBlock Text="下载选项"
                           Foreground="White"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center"
                           Margin="18,0,0,0" />
                <Button Style="{StaticResource MaterialDesignIconButton}"
                        Width="36"
                        Height="36"
                        Padding="6"
                        HorizontalAlignment="Right"
                        Margin="0,3,8,3"
                        Click="Cancel_Click">
                    <materialDesign:PackIcon Kind="Close" Width="20" Height="20" Foreground="White" />
                </Button>
            </Grid>

            <Grid Grid.Row="1" Margin="24,22,24,22">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="92" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Text="文件名" VerticalAlignment="Center" />
                <Grid Grid.Column="1" Grid.ColumnSpan="2" Margin="0,0,0,14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="112" />
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="FileNameTextBox" materialDesign:HintAssist.Hint="文件名" />
                    <TextBlock Grid.Column="1" Text="." VerticalAlignment="Center" Margin="10,0" FontSize="18" Foreground="{DynamicResource MaterialDesignBodyLight}" />
                    <TextBox x:Name="ExtensionTextBox" Grid.Column="2" materialDesign:HintAssist.Hint="扩展名" />
                </Grid>

                <TextBlock Grid.Row="1" Text="保存到" VerticalAlignment="Center" />
                <TextBox x:Name="DirectoryTextBox" Grid.Row="1" Grid.Column="1" Margin="0,0,8,14" />
                <Button Grid.Row="1" Grid.Column="2" Margin="0,0,0,14" Click="Browse_Click">
                    <StackPanel Style="{StaticResource IconButtonPanelStyle}">
                        <materialDesign:PackIcon Kind="FolderOpen" Width="18" Height="18" Margin="0,0,6,0" />
                        <TextBlock Text="选择" />
                    </StackPanel>
                </Button>

                <CheckBox x:Name="AskEveryTimeCheckBox"
                          Grid.Row="2"
                          Grid.Column="1"
                          Grid.ColumnSpan="2"
                          Content="以后每次下载都显示此窗口" />

                <StackPanel Grid.Row="4"
                            Grid.Column="1"
                            Grid.ColumnSpan="2"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right">
                    <Button Click="Start_Click">
                        <StackPanel Style="{StaticResource IconButtonPanelStyle}">
                            <materialDesign:PackIcon Kind="Download" Width="18" Height="18" Margin="0,0,6,0" />
                            <TextBlock Text="开始下载" />
                        </StackPanel>
                    </Button>
                    <Button Margin="8,0,0,0" Click="Cancel_Click">
                        <TextBlock Text="取消" />
                    </Button>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
